# 人力资源管理系统 - React前端

## 📋 项目说明

这是人力资源管理系统的React前端项目，基于后端API接口开发。

**注意：** 旧的JavaScript前端代码已删除，现在只使用React前端。

## 🚀 快速开始

### 1. 安装依赖

```bash
cd ui-react
npm install
```

### 2. 启动开发服务器

```bash
npm run dev
```

前端将在 `http://localhost:8000` 启动

### 3. 启动后端服务

确保后端服务运行在 `http://localhost:8080`

## 📁 项目结构

```
ui-react/
├── src/
│   ├── components/          # 公共组件
│   │   └── Loading.jsx
│   ├── contexts/           # React Context
│   │   └── AuthContext.jsx
│   ├── layouts/            # 布局组件
│   │   └── MainLayout.jsx
│   ├── pages/              # 页面组件
│   │   ├── Login.jsx       # 登录页
│   │   ├── system/         # 系统管理
│   │   ├── archive/        # 员工档案管理
│   │   ├── salary/         # 薪酬标准管理
│   │   └── issuance/       # 薪酬发放管理
│   ├── routes/             # 路由配置
│   │   └── AppRoutes.jsx
│   ├── services/           # API服务层
│   │   ├── api.js          # Axios配置
│   │   ├── authService.js
│   │   ├── organizationService.js
│   │   ├── positionService.js
│   │   ├── salaryItemService.js
│   │   ├── employeeArchiveService.js
│   │   ├── salaryStandardService.js
│   │   └── salaryIssuanceService.js
│   ├── App.jsx
│   └── main.jsx
├── package.json
├── vite.config.js
└── README.md
```

## 🔧 技术栈

- **React 18** - UI框架
- **React Router 6** - 路由管理
- **Ant Design 5** - UI组件库
- **Axios** - HTTP客户端
- **Vite** - 构建工具
- **dayjs** - 日期处理

## 📡 API配置

API基础URL在 `src/services/api.js` 中配置：

```javascript
const API_BASE_URL = 'http://localhost:8080/api'
```

## 🔐 认证机制

- 使用JWT Token进行认证
- Token存储在localStorage中
- 请求拦截器自动添加Authorization头
- Token过期自动跳转到登录页

## 🎯 功能模块

### 系统管理
- ✅ 一级机构管理
- ✅ 二级机构管理
- ✅ 三级机构管理
- ✅ 职位管理
- ✅ 薪酬项目管理

### 员工档案管理
- ✅ 档案登记
- ✅ 档案复核
- ✅ 档案查询
- ✅ 删除管理

### 薪酬标准管理
- ✅ 标准登记
- ✅ 标准复核
- ✅ 标准查询
- ✅ 标准变更

### 薪酬发放管理
- ✅ 发放登记
- ✅ 发放复核
- ✅ 发放查询

## 🛠️ 开发说明

### 添加新页面

1. 在 `src/pages` 下创建页面组件
2. 在 `src/routes/AppRoutes.jsx` 中添加路由
3. 在 `src/layouts/MainLayout.jsx` 中添加菜单项（如需要）

### API调用

使用对应的Service文件：

```javascript
import { organizationService } from '../services/organizationService'

// 调用API
const response = await organizationService.getLevel1List()
if (response.code === 200) {
  // 处理数据
}
```

### 权限控制

使用 `useAuth` Hook：

```javascript
import { useAuth } from '../contexts/AuthContext'

const { user, hasRole, isAuthenticated } = useAuth()

// 检查角色
if (hasRole(['HR_MANAGER'])) {
  // 显示内容
}
```

## 📝 注意事项

1. **日期处理**：使用 `dayjs` 处理日期格式
2. **表单验证**：使用Ant Design的Form组件进行验证
3. **错误处理**：所有API调用都有错误处理
4. **加载状态**：使用loading状态提升用户体验

## 🔄 与后端对接

1. 确保后端服务已启动（端口8080）
2. 确保后端CORS配置正确
3. 前端会自动处理Token认证
4. API响应格式统一为 `{ code, message, data, timestamp }`

## 📦 构建生产版本

```bash
npm run build
```

构建产物在 `dist` 目录中。

## 📚 相关文档

- `启动说明.md` - 详细启动指南
- `对接测试.md` - 前后端对接测试指南
- `项目说明.md` - 项目详细说明
